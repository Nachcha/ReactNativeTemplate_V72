import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
    borderWidth: 1,
    borderColor: 'red',
  },
  textInput: {
    width: '80%',
    height: 50,
    margin: 10,
  },
});

export default styles;
