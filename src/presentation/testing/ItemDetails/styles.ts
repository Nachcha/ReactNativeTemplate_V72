import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: '100%',
    height: 300,
    resizeMode: 'cover',
  },
  topic: {
    color: 'black',
    fontWeight: 'bold',
    fontSize: 18,
    padding: 16,
  },
  description: {
    color: 'black',
    fontSize: 18,
    padding: 16,
  },
});

export default styles;
